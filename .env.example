LM_STUDIO_BASE_URL=http://localhost:1234/v1
LM_STUDIO_API_KEY=lm-studio
# LM_STUDIO_MODEL=  # optional: override auto-selected model from LM Studio
# VISUAL_DISABLED=true  # set to true to disable screenshots, snapshots and post-run comparison
# VISION_MAX_WIDTH=512  # max image size (px) before sending to vision model, fit inside NxN (default: 512)
