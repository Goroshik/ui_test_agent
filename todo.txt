screenshots got taken, but there are no records in memory



need to make a separate agent that will go through the screenshots and update memory, and that will be run manually, separately



I don't want to tie memory to screenshots, I want to tie it to URLs

and maybe it makes sense to store info about what's on the pages separately


=== CONVERSATION ANALYZER (loop detector) ===

[ ] Create an agent hang detector:
    - every 10 messages, take the whole conversation
    - check the last 3+ steps for repetition of the same action
      (for example "click on field", "select data")
    - when a hang is detected — stop execution of the main task

[ ] Add MongoDB:
    - store conversation history
    - store detected loops (loop events)
    - store memory about URLs and pages

[ ] Link the hang detector with the step analyzer:
    - when a loop is detected — don't just stop, try to resolve it
    - the step analyzer suggests a specific action:
      which tool to use or exactly how to click the button
    - pass the loop context to the step analyzer to generate a hint
